stages:
  - build

.build: &build
  stage: build
  script: 
    - cd docs
    - make html
  artifacts:
    paths:
      - docs/build/html

build:docker:
  <<: *build
  image: sphinxdoc/sphinx
  needs: [ ]
